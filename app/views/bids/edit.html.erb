<%= turbo_frame_tag "modal" do %>
  <main class="new-bid">
    <div class="container">
      <div class="top">
        <div>
          <div class="title">Update Bid</div>
        </div>
        
      </div>
      <%= render "form", bid: @bid, url: item_bid_path(@item, @bid) %>
      <%= link_to "Close", :back, class: "back" %>
    </div>
  </main>
<% end %>
